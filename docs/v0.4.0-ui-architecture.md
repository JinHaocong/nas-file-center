# v0.4.0 UI Architecture Audit & Implementation Plan

> Status: C0 frozen implementation plan
> Source baseline: `08ce4046a556e7f3dec1d2de1a7d4ea37c610f7f`
> Design reference pin: `VoltAgent/awesome-design-md@8147538b4226ae41e2487a9179e3bcc1f68e8554`

## Current frontend

Stack:
- React 18.3.1
- TypeScript 5.7.3
- Vite 6.2.0
- Ant Design 5.24.2
- TanStack Query 5.67.1
- React Router DOM 6.29.0
- ECharts 5.6.0

Current shell is `MainLayout → Sidebar + sticky Header + Outlet`.
Theme uses AntD default/dark algorithms with only a primary color, radius and system font override.

## Audit findings

### Responsive gaps

Fresh source search found no shared:
- `useBreakpoint`,
- CSS `@media` contract,
- mobile navigation Drawer,
- `ResponsiveDataView`.

The shell is desktop-first: sidebar is always mounted, content has desktop margins, header action clusters assume horizontal room.

### Styling fragmentation

Representative pages rely heavily on inline styles. Spacing, radii, surfaces and colors are duplicated across shell, Dashboard, Settings and operational pages. This blocks global responsive rules and makes light/dark parity hard to audit.

### Status fragmentation

Strengths:
- `STATUS_MAP`
- `SafeModeBadge`
- `WorkerStatusBadge`
- explicit destructive confirmations

Gaps:
- status presentation is split across Tag presets and local colors,
- lifecycle and risk levels lack shared visual primitives,
- legacy status map does not yet model every current state such as `expired`.

### Mobile risk

Primary pages contain wide Tables, Descriptions, action groups, long absolute paths and dense technical text. Generic horizontal scrolling is rejected for primary workflows.

### Legacy chrome

Sidebar still displays `v0.3.5 Enterprise`.
The UI mixes arbitrary radii and page-local colors.

## Frozen decisions

### Keep Ant Design 5

Retain Ant Design 5 for v0.4.0.

Reasons:
- mature forms/tables/modals/drawers,
- existing behavior is integrated,
- lower functional regression risk,
- design goals can be reached with tokens, CSS variables and NFC wrapper components.

MUI migration is not a success criterion.

### Add project-owned design layer

Add:
- root `DESIGN.md`,
- semantic CSS variables,
- responsive hooks/primitives,
- shared status/safety primitives,
- shared page layout/data primitives.

AntD becomes an implementation detail below the product design layer.

### Preserve business architecture

C1 requires no route/backend API changes.
Do not alter executor, Worker, Plan lifecycle, filesystem operations, Quarantine transaction semantics, PathGuard or mutation authority.

## C1 Foundation

Target:
- `frontend/src/contexts/ThemeContext.tsx`
- `frontend/src/index.css`
- `frontend/src/layouts/MainLayout.tsx`
- `frontend/src/components/Header.tsx`
- `frontend/src/components/Sidebar.tsx`

Add:
- `frontend/src/design/tokens.ts`
- `frontend/src/hooks/useResponsive.ts`
- responsive navigation / shell helpers
- `PageHeader`

Behavior:
- desktop persistent sidebar,
- tablet collapsible navigation,
- mobile Drawer navigation,
- mobile compact header,
- semantic CSS surfaces,
- >=44px primary touch controls,
- safety/worker state remains reachable.

## C2 Shared primitives

Add:
- StatusBadge
- SafetyBadge
- PageSection
- MetricCard
- DataPanel
- ActionBar
- ResponsiveDataView
- ResponsiveDescriptions
- CodePath
- shared Empty/Loading/Error wrappers.

## C3 Representative migration

Migrate:
1. Dashboard
2. Task Center
3. Plans / Plan Detail

Validate:
- desktop/tablet/mobile,
- light/dark parity,
- status hierarchy,
- danger actions,
- charts/data density,
- no lost actions.

Broad migration starts only after these pages validate the system.

## C4 Core data/safety

Scans / Scan Detail / Dedupe, Quarantine, Audit, Indexes.

## C5 Complex tools

Workflows, Organizer, Batch, Path Match, Rename.

## C6 Remaining

Settings, Login, DirectoryPicker and remaining dialogs/drawers.

## Testing

For shared behavior:
RED contract test → minimum GREEN → full frontend tests → typecheck → production build.

Responsive acceptance:
360/390/430, 768/820/1024, 1280/1440/1920; Chrome/Safari; light/dark/system; keyboard/reduced motion.

Backend full regression is required at release closure and whenever an API contract changes.

## Security carry-over

Before v0.4.0 release:
- resolve or explicitly disposition React Router advisory carry-over,
- resolve or explicitly disposition ECharts advisory carry-over,
- verify no external CDN runtime dependency.

## C1 TDD contract

First implementation slice must prove:
- mobile uses Drawer navigation rather than fixed Sider,
- desktop retains persistent sidebar,
- header exposes mobile menu trigger,
- safety/worker state remains reachable,
- content uses semantic responsive gutters,
- theme maps project semantic tokens into AntD,
- routes remain unchanged.


## C2A Visual Prototype — 2026-09-18

Status: IMPLEMENTED / AUTOMATED GREEN / pending manual browser visual acceptance.

Applied the frozen design reference synthesis to a real representative surface:
- Sidebar navigation grouped by product domain instead of one flat 13-item list.
- Dashboard moved away from default Ant Design dashboard-template composition.
- Removed decorative donut chart and nested inner Cards.
- Added shared PageHeader, MetricCard, DataPanel and StatusBadge primitives.
- Added a hairline/surface-first Dashboard layout inspired by the frozen Linear + Supabase direction.
- Applied Carbon-style density and status treatment to operational tables.
- Added dedicated mobile activity cards rather than compressing desktop tables into 360–430px.
- Preserved snapshot freshness wording, refresh cadence and routes to scans/tasks/indexes/organizer.

TDD evidence:
- RED run: GitHub Actions `35297247706` — 395 pass / 5 expected contract failures.
- GREEN run: GitHub Actions `35297454472` — 400 / 400 tests PASS, typecheck PASS, production build PASS.
- Verified product-code candidate: `d923204dbb409e9ddde15db1081d7cabe305059d`.

Not yet claimed:
- manual Chrome/Safari visual acceptance at 390px and 1440px,
- final typography/color tuning on real NAS data,
- representative Task Center / Plans migration.

Next implementation slice:
- extend the same shared primitives and visual language to Task Center, Plans and Plan Detail before broad page migration.


## C2B Operations Surfaces — 2026-09-18

Status: IMPLEMENTED / AUTOMATED GREEN / pending manual browser visual acceptance.

Migrated the representative operational surfaces onto the v0.4.0 visual layer:
- Task Center
- Plans list
- Plan Detail

Shared primitives added:
- `ActionBar`
- `ResponsiveDataView`
- `ResponsiveDescriptions`
- `CodePath`

Task Center:
- keeps active-task 3s polling and local elapsed ticker,
- keeps Worker status, cleanup, delete and detail Drawer semantics,
- replaces the legacy Card/table shell with a hairline DataPanel,
- adds dedicated mobile task cards and mobile pagination.

Plans:
- keeps legacy cleanup, history cleanup and plan delete policy,
- keeps 3s refresh while validating/executing,
- uses StatusBadge and dedicated mobile plan cards.

Plan Detail:
- preserves Freeze → Validate → Execute action conditions,
- preserves ALLOW_MUTATION=false execution lock,
- preserves Operation Journal, Undo generation and Stale Rebuild,
- moves actions into a dedicated safety action surface,
- adds a responsive lifecycle strip,
- uses responsive descriptions for plan metadata,
- uses desktop table / mobile plan-item cards for operation items,
- uses CodePath for source/target/keep paths.

TDD evidence:
- RED run: GitHub Actions `35298009969` — 406 total, 401 pass / 5 expected contract failures.
- GREEN run: GitHub Actions `35298315904` — 406 / 406 tests PASS, typecheck PASS, production build PASS.
- Verified product-code candidate: `f9e725ecd1de63da3f3a52fd35fedac89442257b`.

Not yet claimed:
- manual Chrome/Safari visual acceptance at 390px / 1440px,
- broad migration of Scans / Dedupe / Quarantine / Audit / Indexes,
- complex tools migration.

Next slice:
- C4 core data/safety surfaces: Scans, Scan Detail, Advanced Dedupe, Quarantine, Audit and Indexes.


## C4A Scans & Advanced Dedupe — 2026-09-18

Status: IMPLEMENTED / AUTOMATED GREEN / pending manual browser visual acceptance.

Migrated the scan and advanced dedupe path:
- Scans list
- Scan Detail
- Advanced Dedupe
- Dedupe preview member view

Behavior preserved:
- active Scan 3s polling,
- terminal/dependent-plan Scan delete policy,
- scan creation and DirectoryPicker flow,
- classic DedupePlanModal entry,
- Advanced Dedupe entry,
- dedupe scorer state machine,
- config generation correlation,
- acceptedPreviewDigest authority,
- expected_preview_digest plan-generation contract,
- PREVIEW_CHANGED handling,
- Identity/Safety panel,
- Preview Summary panel,
- Explain drawer,
- Draft-only plan creation followed by Freeze -> Validate -> Execute.

Responsive changes:
- Scan list uses desktop table / mobile scan cards.
- Scan Detail uses responsive descriptions, dedicated duplicate-group cards and expandable member paths.
- Dedupe preview uses desktop table / mobile member decision cards.
- Source/root paths use CodePath.
- Legacy Card wrappers were removed from the migrated pages.

TDD evidence:
- RED run: GitHub Actions `35298755176` — 411 total, 406 pass / 5 expected contract failures.
- Initial GREEN run `35298954238`: tests green, typecheck exposed one pagination union narrowing issue.
- Final GREEN run: GitHub Actions `35298992851` — 411 / 411 tests PASS, typecheck PASS, production build PASS.
- Verified product-code candidate: `9dd945b7e5ff82bde918f8ed700477488e95b45e`.

Not yet claimed:
- manual Chrome/Safari visual acceptance at target widths,
- Quarantine / Audit / Indexes migration.

Next slice:
- C4B safety/data surfaces: Quarantine, Audit and Indexes.


## C4B Quarantine, Audit & Indexes — 2026-09-18

Status: IMPLEMENTED / AUTOMATED GREEN / pending manual browser visual acceptance.

Migrated the remaining C4 core data/safety surfaces:
- Quarantine
- Audit
- Indexes

Quarantine:
- preserves Quarantine-first semantics,
- preserves explicit-ID bulk selection and filtered-ID resolution,
- preserves RestoreModal / PurgeConfirmModal,
- preserves BulkRestoreModal / BulkPurgeModal plan-generation flow,
- preserves admin-only permanent delete gate,
- preserves ALLOW_MUTATION=false and ALLOW_DELETE=false gates,
- uses desktop table / dedicated mobile quarantine cards,
- uses CodePath for original and quarantine paths.

Audit:
- preserves retention-policy display,
- preserves server-backed search through auditApi.listEvents,
- preserves selected-event detail modal and JSON evidence,
- uses desktop table / mobile audit-event cards.

Indexes:
- preserves DirectoryPicker create/reindex flow,
- preserves path_state === available gate for reindex,
- preserves can_remove gate for index metadata removal,
- keeps copy explicit that index removal does not delete real NAS files/directories,
- uses desktop table / mobile index-root cards.

TDD evidence:
- RED run: GitHub Actions `35299284143` — 415 total, 411 pass / 4 expected contract failures.
- Initial GREEN run `35299396590`: behavior tests green; typecheck exposed an extra unsupported PurgeConfirmModal prop.
- Final GREEN run: GitHub Actions `35299465947` — 415 / 415 tests PASS, typecheck PASS, production build PASS.
- Verified product-code candidate: `519fa25b7dfe98fa6795772fccaa8e4dbe0c31da`.

Scope verification:
- no backend/API/router/Worker/executor/fs_ops changes,
- no Quarantine authority or deletion semantics changes,
- all C4B differences are frontend presentation, responsive behavior, tests and temporary CI.

Not yet claimed:
- manual Chrome/Safari visual acceptance at target widths.

C4 core data/safety migration is now complete. Next slice:
- C5 complex tools: Workflows, Organizer, Batch, Path Match and Rename.


## C5A Workflows & Organizer — 2026-09-18

Status: IMPLEMENTED / AUTOMATED GREEN / pending manual browser visual acceptance.

Migrated the complex automation surfaces:
- Workflow list
- Workflow Builder
- Workflow Utility Preview
- Workflow file/organizer/dedupe Preview
- Organizer shell
- Organizer Profile list
- Organizer Preview

Workflow behavior preserved:
- create/archive RBAC,
- builtin/archived/historical read-only states,
- expected_current_revision revision concurrency,
- RevisionDrawer and rollback flow,
- dirty-state exit protection,
- explicit Utility mode and single_child_wrapper_collapse topology,
- Preview state machine,
- runtime root / completed-scan inputs,
- compile_digest authority,
- PREVIEW_CHANGED invalidation,
- dedupe_summary authority,
- explicit selected_candidate_ids for Utility,
- unsupported-filesystem candidates remain non-selectable,
- no rename fallback for unsupported strict no-overwrite MOVE,
- Generate creates Draft only.

Organizer behavior preserved:
- clone/import/export/delete profile actions,
- builtin profiles remain non-editable,
- DirectoryPicker root selection,
- snapshot_id reuse across filters/pages,
- conflict count blocks Plan generation,
- include_touch follows ordered mtime mode,
- Preview remains read-only.

Responsive changes:
- Workflow list uses desktop table / mobile workflow cards.
- Builder uses product DataPanels and responsive mode selection grid.
- Utility candidates use desktop table / mobile selectable candidate cards.
- File/Organizer workflow preview uses desktop table / mobile operation cards.
- Organizer profiles use desktop table / mobile profile cards.
- Organizer proposal preview uses MetricCard summary and desktop table / mobile proposal cards.
- paths use CodePath and primary complex forms collapse to one column on mobile.

TDD evidence:
- Initial C5A RED run: GitHub Actions `35300136907` — 421 total, 415 pass / 6 expected contract failures.
- First GREEN compatibility run `35300447997`: 419 / 421; exposed two frozen Gate6-B source-contract regressions (explicit utility mode and rename-fallback wording).
- Core GREEN run `35300526715`: 421 / 421 tests PASS, typecheck PASS, production build PASS.
- Legacy Preview RED run `35300642186`: 422 total, 421 pass / 1 expected legacy-preview migration failure.
- Final GREEN run `35300790485`: 422 / 422 tests PASS, typecheck PASS, production build PASS.
- Verified product-code candidate: `73d3225a9f55e11f9a16de2b15e90dedfb468364`.

Scope verification:
- no backend/API/router/Worker/executor/fs_ops changes,
- no workflow compiler/generate authority changes,
- no organizer planning semantics changes.

Not yet claimed:
- manual Chrome/Safari visual acceptance at target widths.

Next slice:
- C5B utility tools: Batch, Path Match and Rename.


## C5B Batch, Path Match & Rename — 2026-09-18

Status: IMPLEMENTED / AUTOMATED GREEN / pending manual browser visual acceptance.

Migrated the remaining C5 file-tool surfaces:
- Batch
- Path Match
- Rename

Batch behavior preserved:
- Quarantine / Touch accept explicit paths,
- Move / Rename parse explicit source -> target mappings,
- Plan kind remains batch-${operation},
- each item keeps the selected operation,
- hardlink and reflink options remain disabled / unimplemented,
- Quarantine remains non-destructive and Plan-first,
- generated Plan still requires Freeze -> Validate -> Execute.

Path Match behavior preserved:
- Preview uses batchApi.previewPathMatch,
- at least two roots are required,
- relative-path / basename / stem / normalized modes are preserved,
- first member of each group remains the keep path,
- subsequent members become quarantine Plan items with expected_size,
- plan kind remains path-match-dedupe,
- Preview does not mutate files.

Rename behavior preserved:
- Preview uses batchApi.previewRename,
- regex / prefix / suffix / numbering / include_parent remain intact,
- source_extension and target_extension remain in the Preview payload,
- extension replacement remains rename-only and does not transcode WebP -> JPEG,
- any proposal conflict blocks Plan generation,
- generated Plan kind remains rename.

Responsive changes:
- complex forms use DataPanel / ActionBar and mobile one-column behavior,
- Batch operation choices use a responsive operation grid,
- Path Match uses desktop table / mobile group cards,
- Path Match member paths use CodePath and retain keep/copy state,
- Rename uses desktop table / mobile proposal cards,
- source/target paths use CodePath and conflict state uses StatusBadge.

TDD evidence:
- RED run: GitHub Actions `35300977555` — 426 total, 422 pass / 4 expected contract failures.
- Final GREEN run: GitHub Actions `35301116071` — 426 / 426 tests PASS, typecheck PASS, production build PASS.
- Verified product-code candidate: `999af0363091c93f70c754753981b3b74f54e6df`.

Scope verification:
- no backend/API/router/Worker/executor/fs_ops changes,
- no Plan payload or file-operation semantics changes,
- all C5B differences are frontend presentation, responsive behavior, tests and temporary CI.

Not yet claimed:
- manual Chrome/Safari visual acceptance at target widths.

C5 complex tools migration is now complete. Next slice:
- C6 remaining surfaces: Settings, Login, DirectoryPicker and remaining dialogs/drawers.


## C6A Settings, Login & DirectoryPicker — 2026-09-18

Status: IMPLEMENTED / AUTOMATED GREEN / pending manual browser visual acceptance.

Migrated the remaining global entry and settings foundations:
- Login
- Settings
- DirectoryPicker
- DirectoryPickerModal responsive shell

Login:
- removed legacy gradient / marketing-card treatment,
- uses restrained hairline product surface,
- preserves authentication redirect/login behavior,
- preserves Light / Dark / System theme selection,
- updates visible product version to v0.4.0.

Settings:
- uses PageHeader + DataPanel product surfaces,
- uses ResponsiveDescriptions for safety, retention and resource state,
- uses ResponsiveDataView for administrator sessions,
- adds dedicated mobile session cards,
- preserves ALLOW_MUTATION / ALLOW_DELETE read-only display,
- preserves admin-only retention/resource mutations,
- preserves audit retention fresh-preview confirmation flow,
- preserves quarantine retention semantics,
- preserves resource-policy validation,
- preserves session revoke behavior.

DirectoryPicker:
- replaces Card/tag-heavy multi-selection with path-first hairline rows,
- uses CodePath for selected technical paths,
- preserves manual multi-line input,
- preserves single/multiple selection contracts,
- DirectoryPickerModal preserves browsing, favorites, recent-path recording and selection semantics,
- adds responsive modal sizing and toolbar surface.

TDD evidence:
- RED run: GitHub Actions `35301681286` — 432 total, 427 pass / 5 expected visual-contract failures.
- GREEN run: GitHub Actions `35301912575` — 432 / 432 tests PASS, typecheck PASS, production build PASS.
- Verified product-code candidate: `a12d993f85a70c6fb00e47e14b3879c3ea96cf9c`.

Scope verification:
- no backend/API/router/Worker/executor/fs_ops changes,
- no setting authority or filesystem browsing semantics changes.

Not yet claimed:
- manual Chrome/Safari visual acceptance at target widths.

Next slice:
- C6B remaining dialogs/drawers and overlay consistency.


## C6B Overlay Consistency — 2026-09-18

Status: IMPLEMENTED / AUTOMATED GREEN / pending manual browser visual acceptance.

Unified the remaining named Modals and Drawers under the v0.4.0 overlay chrome:
- DedupeExplainDrawer
- StaleRebuildDrawer
- TaskDetailDrawer
- RevisionDrawer
- OperationJournalDrawer
- ChangePasswordModal
- DirectoryPickerModal
- PlanHistoryCleanupModal
- TaskHistoryCleanupModal
- Organizer ProfileFormModal
- Quarantine BulkPurgeModal
- Quarantine BulkRestoreModal
- Quarantine PurgeConfirmModal
- Quarantine RestoreModal
- Scans DedupePlanModal

Visual behavior:
- shared restrained hairline modal/drawer surfaces,
- shared header/footer spacing and typography,
- mobile drawers use full-width responsive behavior,
- mobile modals use compact viewport-aware width,
- overlay content remains scrollable without page-level overflow.

Safety/behavior preserved:
- Quarantine purge still requires admin + ALLOW_MUTATION + ALLOW_DELETE,
- bulk purge retains expected_preview_digest and typed DELETE confirmation,
- bulk restore retains expected_preview_digest and conflict policy,
- task detail retains cancel/pause/resume controls,
- operation journal keeps plan-bound operation evidence,
- RevisionDrawer keeps expected_current_revision and rollback authority,
- all existing confirmation copy and mutation conditions remain unchanged.

TDD evidence:
- RED run: GitHub Actions `35302116937` — overlay contract failed on missing shared chrome as expected.
- Intermediate runs exposed literal escaped-newline JSX formatting defects only.
- Final GREEN run: GitHub Actions `35303357244` — 438 / 438 tests PASS, typecheck PASS, production build PASS.
- Verified product-code candidate: `749345310f3dbef703d3569f7d9a9f9827a7c1ac`.

Scope verification:
- no backend/API/router/Worker/executor/fs_ops changes,
- no Quarantine, Plan, Task or Workflow authority changes.

C6 implementation migration is now complete.

Remaining v0.4.0 closure:
- cross-page visual consistency review,
- manual target-width acceptance,
- Chrome/Safari light/dark/system checks,
- accessibility/keyboard/reduced-motion review,
- dependency/security disposition,
- release Docker/NAS smoke and version closure.
